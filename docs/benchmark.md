# Benchmark

Generated by `scripts/bench.py` on this machine. Numbers are
candidate evaluations of tiny Python fixtures, not a production
test suite.

| Fixture | Input hunks | Input lines | Output hunks | Output lines | Trials | PASS | FAIL | UNRESOLVED | Cache | Seconds |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A single bad hunk | 26 | 58 | 1 | 2 | 8 | 3 | 5 | 0 | 0 | 0.26 |
| B interacting pair | 5 | 10 | 2 | 4 | 13 | 3 | 2 | 0 | 8 | 0.20 |
| C invalid subsets | 5 | 9 | 3 | 5 | 28 | 5 | 2 | 5 | 16 | 0.35 |
| D multiple minima | 5 | 10 | 1 | 2 | 5 | 1 | 3 | 0 | 1 | 0.13 |
| E confirm=3 | 4 | 8 | 1 | 2 | 4 | 1 | 2 | 0 | 1 | 0.37 |
| F messy refactor | 20 | 67 | 2 | 31 | 41 | 9 | 5 | 3 | 24 | 0.84 |
| G dense interacting | 7 | 14 | 4 | 8 | 44 | 17 | 2 | 0 | 25 | 0.58 |
| demo 400+ lines | 93 | 567 | 1 | 2 | 8 | 2 | 6 | 0 | 0 | 0.36 |

## Reduction ratio

The original demo is the easy shape: noise in other files,
one tiny operator flip. Fixture F is the harder one: a fat
fused rewrite plus a new module, with a naive reproduce
script that does **not** use exit 125.

## Demo report

```
Minimal failure-inducing change set:

calc.py
  hunk 2  def total(items):
    -        acc = acc + item
    +        acc = acc - item

Full working tree: FAIL  14 files, 93 hunks, 567 changed lines
Reduced patch:     FAIL  1 files, 1 hunks, 2 changed lines
Reduction:         93 -> 1 hunks (99% fewer), 567 -> 2 lines

Removing any remaining hunk:
  (single remaining hunk; HEAD already verified PASS)

Trials: 8  (PASS 2, FAIL 6, UNRESOLVED 0, cache hits 0)
Elapsed: 0.36s

Note: uniqueness is not guaranteed: another 1-minimal subset may also FAIL

This is a minimal failure-inducing change set, not 'the' cause. Multiple hunks may interact; another 1-minimal subset may also fail.
```

## Demo reduced patch

```diff
diff --git a/calc.py b/calc.py
--- a/calc.py
+++ b/calc.py
@@ -10 +10 @@ def total(items):
-        acc = acc + item
+        acc = acc - item
```

## Fixture F — messy refactor

```
Minimal failure-inducing change set:

checkout.py
  hunk 1  from pricing import line_total
    -def total(items):
    -    acc = 0
    -    for qty, price in items:
    -        acc += line_total(qty, price)

promo.py
  hunk 1
    +"""Promotions extracted during the WIP refactor."""
    +
    +def apply_promo(amount):
    +    # Identity on purpose: the failure is the -1 in checkout,

Full working tree: FAIL  8 files, 20 hunks, 67 changed lines
Reduced patch:     FAIL  2 files, 2 hunks, 31 changed lines
Reduction:         20 -> 2 hunks (90% fewer), 67 -> 31 lines

Removing any remaining hunk:
  without checkout.py#1: PASS
  without promo.py#1: UNRESOLVED
  1-minimal: yes (dropping any leftover hunk PASSes or will not run)

Trials: 41  (PASS 9, FAIL 5, UNRESOLVED 3, cache hits 24)
Elapsed: 0.84s

Note: uniqueness is not guaranteed: another 1-minimal subset may also FAIL

This is a minimal failure-inducing change set, not 'the' cause. Multiple hunks may interact; another 1-minimal subset may also fail.
```

## Follow-up notes

- Fixture F reproduce.sh does **not** map import errors to 125.
  3 trials were UNRESOLVED via the Python traceback heuristic.
  `--strict-exits` on the same tree reduces to `checkout.py` only
  (an ImportError, not the assertion). That is the poison the
  heuristic exists to prevent.
- The leftover checkout hunk is fat (~23 lines). The off-by-one
  sits in the middle of a contiguous rewrite; hunk-level cannot
  extract the single `- 1`. That is the v0.1 ceiling, reported
  honestly.
- Fixture G keeps all 4 required bits and drops noise. Reduction
  can be real without being dramatic.
